export function Doom({ mode, openPlayground }: { mode: "mock" | "live"; openPlayground: () => void }) {
  return <div className="doom-app"><div className="doom-mark">DOOM</div><h2>JAM DOOM</h2><p>DOOM is not installed yet. This window is reserved for a real MiniJAM execution path.</p><div className="doom-status"><span>Runtime</span><strong>{mode === "live" ? "LIVE MiniJAM" : "DEMO mock"}</strong></div><div className="doom-steps"><span>1 · Build a DOOM-compatible PVM workload</span><span>2 · Deploy/register the execution service</span><span>3 · Bridge input, frames and game state to this window</span></div><button className="login-button" onClick={openPlayground}>Open Playground</button><small className="coming-soon">No client-only DOOM is shipped or presented as JAM execution.</small></div>;
}
