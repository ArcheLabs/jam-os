import type { WindowInstance } from "./types";
export function Taskbar({ windows, mode, network, onOpen, onFocus }: { windows: WindowInstance[]; mode: string; network: string; onOpen: () => void; onFocus: (id: string) => void }) { return <footer className="taskbar"><button className="start-button" onClick={onOpen}>◆ <span>JAM Computer</span></button><div className="task-items">{windows.map((item) => <button key={item.id} className={item.minimized ? "task-item minimized" : "task-item"} onClick={() => onFocus(item.id)}>{item.title}</button>)}</div><div className="task-status"><span className="health-dot" />{network}<span className="mode-pill">{mode.toUpperCase()}</span><span>{new Date().toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" })}</span></div></footer>; }
