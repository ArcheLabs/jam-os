import type { ComponentType } from "react";
import { JamAppIcon } from "../ui/brand/JamAppIcon";
export function DesktopIcon({ appId, icon, title, selected, onSelect, onOpen }: { appId: string; icon: ComponentType<{ size?: number; strokeWidth?: number }>; title: string; selected: boolean; onSelect: () => void; onOpen: () => void }) { return <button className={`desktop-icon ${selected ? "selected" : ""}`} onClick={(event) => { event.stopPropagation(); onSelect(); }} onDoubleClick={(event) => { event.stopPropagation(); onOpen(); }}><span className="icon-glyph"><JamAppIcon appId={appId} fallbackIcon={icon} size="desktop" /></span><span>{title}</span></button>; }
