import type { ComponentType } from "react";
import { JamAppIcon } from "../ui/icons";
export function DesktopIcon({ appId, icon, title, selected, onSelect, onOpen }: { appId: string; icon: ComponentType<{ size?: number; strokeWidth?: number }>; title: string; selected: boolean; onSelect: () => void; onOpen: () => void }) { return <button className={`desktop-icon ${selected ? "selected" : ""}`} onClick={(event) => { event.stopPropagation(); onSelect(); }} onDoubleClick={(event) => { event.stopPropagation(); onOpen(); }}><JamAppIcon appId={appId} context="desktop" fallbackIcon={icon} /><span className="desktop-icon-label">{title}</span></button>; }
