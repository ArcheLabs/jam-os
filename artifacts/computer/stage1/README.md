# Computer Stage-1 artifact

`scriptc/` is the reviewed JamScript 0.2 production artifact. Its ABI is
`services/computer/abi/service.abi.json`; the content-addressed code hash is
`0x375532e134f651d46350a3a66d6206841a1677fa696354310f7f070d113fb895`.

The sibling `service.blob` and `service.polkavm` files are retained as the
legacy C protocol fixture and are not the production deployment target.
