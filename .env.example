# Local development defaults to the isolated mock runtime.
VITE_JAM_MODE=mock

# Optional live Phase 3C session gateway (WebSocket). Preview uses its local realtime adapter.
# VITE_DOOM_GATEWAY_URL=wss://doom-gateway.example/ws

# Production GitHub Pages values are embedded in
# .github/workflows/deploy-pages.yml and do not require repository variables.
